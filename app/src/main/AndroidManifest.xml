<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.memcreator">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/moments"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustPan">
        </activity>
        <activity
            android:name=".activities.ProfileActivity"
            android:label="Profile" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="Settings" />
        <activity
            android:name=".activities.ViewMemory"
            android:label="Memory" />
        <activity
            android:name=".activities.PromptDeleteConfirmationActivity"
            android:theme="@style/AppTheme.Custom" />
        <activity
            android:name=".activities.EditingMemoryActivity"
            android:label="Edit Memory" />
        <activity android:name=".activities.AddPhotosToMemoryActivity" />
        <activity android:name=".activities.PromptToAddPhotosActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>